
export const highProcess = (iterators) => {
    console.log(iterators);
    for (let i = 0; i < iterators; ++i) {
        console.log("Ahi vamos...");
    }
    return `${iterators} iteraciones realizadas`;
}
